<?php

/**
 * An application plugin resource for configuring the jQuery controller helper
 * We have this, so that we can configure the jQuery controller helper using the
 * application's configuration files.
 */
class ZFE_Resource_Jquery extends Zend_Application_Resource_ResourceAbstract
{
    public function init()
    {
        // Nothing needs to be done here.
    }
}
